// example_mod.js — compiled to example_mod.vjs_c at patch time by the
// Resource Compiler and hooked into the credits screen via xml.json's
// add_script action.
(function () {
  $.Msg('[Example Mod] Panorama script loaded.');
})();